Interactive run -- `metarunnner.py`
Normal run -- `main.py`
Installing -- `pip install -r requirements.txt`

100% CPU -- Python don't like idle cycles forever cycles. Some
recipies from Internet may help. 
